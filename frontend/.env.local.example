# Copy to .env.local locally; on Vercel set these in Project Settings > Environment Variables
# Backend URL for server-side proxy (frontend/src/app/api/research/route.ts:5)
BACKEND_URL=http://127.0.0.1:8000
# Same value exposed to Next.js if needed (fallback)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# For production (after Render deploy):
# BACKEND_URL=https://researchforge-backend.onrender.com
